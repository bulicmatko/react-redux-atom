const { describe, it } = require('mocha')
const { expect } = require('chai')

// const { createReactReduxAtomFactory } = require('../index')

/**
 *  Create React Redux Atom Factory - Test
 */
describe('Create React Redux Atom Factory', () => {
  it('should pass the test', () => {
    const expected = true
    const result = true

    expect(result).to.equal(expected)
  })
})
